/**
 * Moved to `cultivators-the-road-finished.ts`.
 *
 * "Fallen" is evocative and ambiguous. These are cultivators the road already finished with, and what they do afterwards.
 *
 * Re-export rather than a rewrite of every importer, because rewriting an
 * import line in somebody else's file sweeps their unstaged work into your
 * commit. Migrate as those files come free, then delete this.
 */
export * from './cultivators-the-road-finished.js';
