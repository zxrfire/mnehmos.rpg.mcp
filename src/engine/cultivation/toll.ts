/**
 * Moved to `price-of-advancement.ts`, which is what its own header always
 * called it: "The Price of Advancement."
 *
 * Kept as a re-export for the same reason as `escapes.ts` - the importers are
 * mid-edit under other agents. Migrate and delete.
 */
export * from './price-of-advancement.js';
