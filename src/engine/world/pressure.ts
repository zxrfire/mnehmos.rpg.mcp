/**
 * Moved to `the-world-changing-on-its-own.ts`.
 *
 * "Pressure" named a metaphor. The module is the yearly pass over the whole world - advancement, recruitment, promotion, books, disasters, gatherings - i.e. everything that happens while nobody is watching.
 *
 * Re-export rather than a rewrite of every importer, because rewriting an
 * import line in somebody else's file sweeps their unstaged work into your
 * commit. Migrate as those files come free, then delete this.
 */
export * from './the-world-changing-on-its-own.js';
